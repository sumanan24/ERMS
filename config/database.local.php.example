<?php
/**
 * Copy this file to database.local.php and set your server database credentials.
 * database.local.php is not committed to git so each environment can have its own settings.
 *
 * Example for exam.ucj.ac.lk (production):
 */
$db_host = 'localhost';          // or your MySQL server hostname
$db_name = 'exam_management';    // database name
$db_user = 'your_db_username';
$db_pass = 'your_db_password';
